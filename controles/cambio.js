exports.contraseñaEmpleado = function (req, res) {
	// if(req.session.cambio.cambio==='sip'){
	// 	var id = req.session.id;
	// 	var contraseña = req.body.contraseña;
	// 	madb.empleados.findByIdAndUpdate(id, { $set: { 'contraseña' : contraseña }}, function (err, resultado){
	// 		if (err) {
	// 			return next(err);
	// 		}else{
	// 			res.send("cambio exitoso");
	// 		}
	// 	});
	// }else{
	// 	res.send("no");
	// }
}